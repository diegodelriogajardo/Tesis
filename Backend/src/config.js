// config.js
const config = {
  development: {
    username: "sa",
    password: "",
    database: "p1",
    host: "localhost",
    dialect: "mysql",
    port: "3307", // si no sabes cual es es 3306 por defecto
  },
};

export default config;
