// config.js
const config = {
    development: {
        username: 'root',
        password: '',
        database: 'p1',
        host: 'localhost',
        dialect: 'mysql',
        port: '3310',// si no sabes cual es es 3306 por defecto
    },
};

export default config;
